[package]
name = "ralph-island-worker"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
base64 = "0.22.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sim-core = { path = "../sim-core" }
worker = "0.7.4"
